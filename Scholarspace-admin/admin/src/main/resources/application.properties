# Server Configuration
server.port=9090

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ScholarSpace
spring.datasource.username=postgres
spring.datasource.password=Jayjay_1
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
app.jwt.secret=your_secret_key_here_should_be_at_least_32_characters_long
app.jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
app.upload.dir=C:\\ScholarSpace\\uploads\\

# Ensure the uploads directory exists
spring.datasource.initialization-mode=always

# File storage configuration
scholarspace.file.storage.path=C:\\ScholarSpace\\uploads\\

# Logging Configuration
logging.level.root=DEBUG
logging.level.org.springframework=DEBUG
logging.level.com.scholarspace=TRACE
spring.main.allow-circular-references=true